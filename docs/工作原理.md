# TC 工作原理

TC 是给普通创业者使用的 AI 创业解题教练，不是创业百科或赚钱项目库。这份文档回答三个问题：TC 为什么不是一段普通提示词、仓库为什么有这些文件夹、它怎样从混乱叙述走到可执行方案。

## 一句话原理

```text
讲清问题 → 找到最早断点 → 做一次低成本验证 → 根据结果继续调整
```

普通 AI 容易直接回答用户嘴上问的那句话。TC 会先判断“用户想问的”和“用户真正需要解决的”是不是同一件事。

- 如果本来就是同一件事，直接回答；
- 如果问题混乱或存在关键误判，先重构和定义；
- 问题确认后，才给推荐方案；
- 方案必须落到市场动作和可观察证据。

## 主流程

```text
用户的真实叙述
      ↓
区分：事实 / 解释 / 情绪 / 假设
      ↓
检查：赚钱链条最早断在哪里
      ↓
写成：有对象、目标、约束、边界、验证标准的问题
      ↓
用户确认或纠正
      ↓
一个方案 + 一个主要代价
      ↓
一个最小动作 + 一个有效标准
      ↓
用新证据更新下一步
```

赚钱链条按这个顺序检查：

```text
目标用户 → 真实问题 → 可售卖方案 → 获客 → 有效对话
        → 报价 → 付款 → 交付 → 毛利 → 复购/转介绍
```

TC 只优先解决最早断掉的一环。例如还没有任何人愿意付款时，先验证需求，不急着研究自动化和团队扩张。

## 七个 Skill 怎样配合

`/tc` 是总入口。它读取当前对话，判断此刻只需要做哪一步。

```text
                         ┌─ 问题没说清 ─→ tc-diagnosis
用户 ─→ /tc 主入口 ─────┼─ 需要对外表达 → tc-copy
                         ├─ 知道但没行动 → tc-action
                         ├─ 要保存或恢复 → tc-state
                         ├─ 要查或维护知识 → tc-knowledge
                         └─ 需要升级版本 → tc-update
```

这不是固定流水线。TC 不会默认把六个子 Skill 全部跑一遍，而是根据上一步真实结论只选当前一个。

主入口先区分新手、从零项目、新任务、已确认任务和继续旧任务。继续旧任务时，`tc-state` 会从本机存档恢复上次的问题、决定、待验证假设和下一步，不要求用户重新讲完整背景。

从零项目不是让 AI 凭记忆推荐“当前风口”。TC 最多确认生存边界和一个真实工作/项目场景，然后主动给一个项目假设；有联网工具时核对当前对标、用户问题、替代方案、价格和规则，没有联网工具时明确缺少现实证据，并把第一步设为找对标、访谈或报价。项目最终由真实对话、付款和交付决定，不由模型断言。

## 文件夹分工

### `skills/`：真正执行任务

每个 Skill 都有一个 `SKILL.md`。它告诉 AI：什么时候触发、按什么步骤工作、哪些事情不能做、怎样输出。

主入口位于 `skills/tc/`：

```text
skills/tc/
├── SKILL.md           # 核心工作流和路由规则
├── agents/openai.yaml # AI 工具里的展示名称、简介和默认提示
├── assets/tc-lite.txt # 普通聊天 AI 使用的轻量版
└── references/        # 只在相关问题出现时读取的方法、路由与知识包
```

### `references/`：按需翻阅的教材

TC 不会把全部方法每次都塞进上下文。遇到对应任务才读取对应文件：

- 内容问题读取 `content-to-revenue.md`；
- 从零创业读取 `zero-to-one.md`；
- X 起号读取 `x-growth.md`；
- 合作和市场验证读取 `market-loop.md`；
- 外部资料整理读取 `source-distillation.md`；
- 反馈问题读取 `feedback.md`；
- 安装分发读取 `distribution.md`。

这样能减少上下文浪费，也能避免不相关的方法干扰当前判断。

### `知识库/`：知识进入 TC 的规则

知识库只是书架，真正的产品是 TC 的判断能力。这里不是把所有文章堆在一起，而是说明：

- 来源怎样登记；
- 哪些是事实，哪些只是观点；
- 动态信息什么时候必须重新核对；
- 案例怎样脱敏；
- 什么内容可以进入公开 Skill，什么只能留在私有资料里。

`知识库/核心参考源/` 保存两套核心判断框架：技术合伙人的商业与 AI 实操框架、天策的公开实践与创业价值观。它们只提供经过提炼的通用方法，不替代用户事实。`知识库/公开内容索引/` 保存 489 条历史原创内容，`知识库/原子库/` 保存 22 条有日期、证据等级和公开链接的知识卡片，`知识库/Skill知识包/` 保存 6 个按问题装配的专项教材。原推只在用户明确查询作者历史表达时读取，不能参与普通用户的创业项目生成、方向推荐或能力判断。

### `tc-knowledge`：真正面向知识的脚本

`skills/tc-knowledge/scripts/tc_knowledge.py` 可以搜索核心参考源、原推、原子和知识包，也能输出数量、日期范围和校验结果。用户接受上游许可证后，它还可以从 dontbesilent 原仓库同步 DBS Markdown，并按主题、标签和正文搜索 1,544 条外部记录。它与 `tc-state` 分工明确：前者管理资料依据，后者管理某个创业任务的进度。

DBS 内容采用 CC BY-NC 4.0，所以 TC 只保存来源卡与检索能力，不在 Apache-2.0 安装包中重发全文。维护者可以把3至5条相关记录加工成不含正文的本机 L1 研究候选，但候选只有在拿到独立市场证据、反例检查和人工许可审查后，才可能另行形成 TC 方法；验证通过也不会自动改变上游许可。

### `tc-state`：真正面向任务的脚本

`skills/tc-state/scripts/tc_state.py` 不只是说明文字。它可以：

- 保存一份结构化创业状态；
- 列出本机项目；
- 恢复最近一次存档；
- 把多次存档合成阶段报告。
- 在用户单独授权后导出已脱敏、本机保存且仍需人工复核的市场证据候选。

默认只写入 `~/.tc/`，不自动上传。敏感内容保存前必须提醒用户明文风险。

### `docs/`：给人看的说明书

`SKILL.md` 是给 AI 执行的；`docs/` 是给使用者和维护者理解的。两者不能混为一谈。

### `tests/`：行为考试卷

测试不只检查文件能不能打开，还检查 TC 会不会：

- 把简单问题强行复杂化；
- 一上来倾倒长报告；
- 编造数据或收益承诺；
- 把内容流量当成真实收入；
- 忽略合作、平台与隐私边界。

### `tools/`：构建和发版机器

- `check.py`：检查版本、目录、元数据、链接和必要文档；
- `build.py`：生成单 Skill 安装包和完整套件；
- `release.py`：同步所有位置的版本号。

构建阶段仍会生成每个 Skill 的独立 ZIP，用于校验和高级安装；正式 Release 只展示完整套件和 `/tc` 主入口，避免普通用户看到一排压缩包不知道选哪个。想看文件夹结构应进入 GitHub 的 Code 页面，而不是 Release 页面。

## 三层加载方式

TC 使用 Agent Skills 常见的渐进式加载：

1. **元数据**：AI 平时只看到名称和一句用途，用来判断是否触发；
2. **SKILL.md**：触发后才读取主流程；
3. **references / assets**：只有任务确实需要时才读取。

这就是为什么 TC 不是把所有内容写进一个超长文件，也不是把一小段提示词包装成“系统”。

## 知识如何进入 TC

外部帖子、课程、飞书文档或用户历史资料不会被整包视为真理。处理顺序是：

```text
登记来源和日期
   ↓
区分事实、经验、观点、动态判断和不可采用内容
   ↓
提炼可公开的核心参考视角
   ↓
提取会改变判断或行动的方法
   ↓
脱敏、验证、标注适用边界
   ↓
进入私有知识库或公开参考资料
```

历史内容只能证明“当时说过”，不能自动证明“现在仍然正确”。

## 隐私与反馈

TC 不会自动把用户对话发给作者，也不能暗示作者能看到使用记录。

只有用户明确同意时，AI 才能帮助整理脱敏反馈。任何反馈都应删除客户姓名、联系方式、商业机密、账户凭据和其他敏感信息。

## 怎样判断 TC 有没有用

不能只问“回答看起来聪不聪明”。更重要的是：

- 问题是否比原来更清楚；
- 用户是否接受了一个真实取舍；
- 24 小时内是否接触真实市场；
- 是否拿到用户原话、有效对话、报价、付款、毛利或复购证据；
- 新证据是否改变了下一步判断。

如果 TC 只让用户感觉舒服，却没有让现实多发生一点变化，就不算完成任务。
